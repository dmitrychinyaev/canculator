Ошибка заключалась в том, что в переменной devide не было учтено деление на ноль. Это можно исправить, либо с помощью тернарного оператора, либо прописать блок.

 {
   if (y == 0){
       System.out.println("На ноль делить нельзя");
    }else {
        return x / y;
    }
    return 0;
};


 y > 0 ? x / y : 99999999;